class NotificationModel {
  constructor(id, title, time, message, color, icon) {
    this.id = id;
    this.title = title;
    this.time = time;
    this.message = message;
    this.color = color;
    this.icon = icon;
  }
}

export default NotificationModel;
