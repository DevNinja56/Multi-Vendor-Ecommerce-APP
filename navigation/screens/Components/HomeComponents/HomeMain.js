import React from "react";
import { View, Text, StyleSheet, SafeAreaView } from 'react-native';

const HomeMain = (props, { navigation }) => {
    return (
        <SafeAreaView>
            <View style={styles.container}>
                <Text>HomeMain</Text>

            </View>
        </SafeAreaView>
    );
};

const styles = StyleSheet.create({});

export default HomeMain;